# Arazzo Contracts

Place machine-readable multi-step workflows here, especially:

- upload
- completion
- polling
- manifest retrieval

Each workflow should reference the OpenAPI descriptions it depends on and stay aligned with generated SDK helpers.

Current source artifacts:

- `public-upload.arazzo.yaml`
