# Deploy

This directory is for deployment artifacts, reference stack definitions, and environment-specific bring-up files.

The first target is `local-platform/`, which should host the contributor bring-up for the minimal vertical slice described in `docs/conformance.md`.
