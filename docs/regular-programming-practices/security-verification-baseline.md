# Security Verification Baseline

The platform should verify:

- upload authorization and private delivery controls
- replay and idempotency protections
- operator-only actions
- archive/package safety controls
- secret handling and signed URL behavior

Security-sensitive changes should land with evidence, not assumptions.

