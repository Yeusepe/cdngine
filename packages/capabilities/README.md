# Capabilities Package

This package is for capability registration, processor registration, and recipe-binding helpers.

It also owns the format-agnostic normalization contract and the generic fallback capability used when an upload does not match a more specific registered format.

Governing docs:

- `docs/pipeline-capability-model.md`
- `docs/service-registration-model.md`
- `docs/workload-and-recipe-matrix.md`
