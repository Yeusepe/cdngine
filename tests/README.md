# Tests

This directory is for repository-level tests and fixtures that prove the documented platform contracts.

Primary expected areas:

- `conformance/`
- `fixtures/assets/`

See `docs/testing-strategy.md` and `docs/conformance.md`.
